# tmux >= 3.2. Paths are relative to this file, including when loaded with -f.
source-file -F "#{d:current_file}/conf.d/00-options.conf"
source-file -F "#{d:current_file}/conf.d/10-keys.conf"
source-file -F "#{d:current_file}/conf.d/20-copy-mode.conf"
source-file -F "#{d:current_file}/conf.d/30-theme.conf"
